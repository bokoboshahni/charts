apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eve-member-mailer.fullname" . }}-test-connection"
  labels:
    {{- include "eve-member-mailer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eve-member-mailer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
